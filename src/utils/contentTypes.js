import { CONTENT_TYPES, DEFAULT_VALUES } from '../config/settings';

// Re-export content types for backward compatibility
export const ContentType = CONTENT_TYPES;

// Re-export default values
export const DEFAULT_WORK_VALUES = DEFAULT_VALUES.WORK;
export const DEFAULT_BLOG_VALUES = DEFAULT_VALUES.BLOG;
export const DEFAULT_PROJECT_VALUES = DEFAULT_VALUES.PROJECT;
